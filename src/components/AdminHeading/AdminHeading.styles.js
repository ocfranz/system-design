import styled from "styled-components";
import { colors } from "../../styles/colors";

const AdminHeadingStyled = styled.span`
  font-size: 20px;
  color: ${colors.info};
  font-weight: bold;
  display: block;
  padding: 0.5em 1em;
  border-left: 3px solid ${colors.info};
  margin: 0px 0px 10px 0px;
`;
export { AdminHeadingStyled };
