import styled from "styled-component";
import { InputStyled, InputWrapper, InputLabel } from "../Input/Input.styles";

const IconInputStyled = styled(InputStyled)``;
const IconInputWrapper = styled(InputWrapper)``;
const IconInputLabel = styled(InputLabel)``;
export { IconInputLabel, IconInputWrapper, IconInputStyled };
